package pt.pa.Command;
/*
public class ResetCommand implements Command{
    private ShoppingCart cart;

    private ShoppingCart oldCart;

    public ResetCommand(ShoppingCart cart) {
        this.cart = cart;
        this.oldCart=null;
    }

    @Override
    public void execute() {
        this.oldCart= new ShoppingCart(cart);
        cart.reset();
        System.out.println("EXECUTE RESET");
    }

    @Override
    public void unExecute() {
        cart.setCart(oldCart);
    }
}
*/